# Legacy unlocalized tool URLs → English locale (permanent 301).
# Trailing `!` forces the redirect even when a static /tools/* asset exists
# (Cloudflare Pages would otherwise prefer the static file over the redirect).
/tools /en/tools 301!
/tools/ /en/tools 301!
/tools/* /en/tools/:splat 301!
