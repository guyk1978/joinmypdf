export function SocialProofStrip() {
  return (
    <section className="rounded-2xl border border-white/10 bg-gradient-to-br from-white/[0.04] to-transparent px-6 py-8 text-center">
      <p className="text-xs font-semibold uppercase tracking-[0.2em] text-ink-muted">Works where you work</p>
      <p className="mx-auto mt-3 max-w-2xl text-sm text-ink md:text-base">
        Use JoinMyPDF in Chrome, Safari, Firefox, or Edge when you need PDF utilities without routing files through a
        generic upload pipeline.
      </p>
    </section>
  );
}
