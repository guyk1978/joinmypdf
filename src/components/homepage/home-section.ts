/** Max items shown in each homepage carousel section. */
export const HOME_SECTION_MAX_ITEMS = 20;

/** Cards advanced per arrow click. */
export const HOME_SECTION_ARROW_STEP_CARDS = 3;

/**
 * Continuous marquee speed while play mode is active (CSS pixels per second).
 * Kept intentionally slow so titles stay readable.
 */
export const HOME_SECTION_MARQUEE_PX_PER_SEC = 36;
