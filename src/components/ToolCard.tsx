/**
 * Canonical global tool card — ultra-minimal accent + title + maximize.
 */
export {
  MinimalToolCard as ToolCard,
  MinimalToolCard,
  type MinimalToolCardProps as ToolCardProps,
  type MinimalToolCardProps,
} from "@/components/MinimalToolCard";
