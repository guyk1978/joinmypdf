/** Initial tool cards shown per category — keeps complete grid rows (3×2 on desktop). */
export const TOOL_GRID_INITIAL_VISIBLE = 6;

/** Additional cards revealed per "Show more" click. */
export const TOOL_GRID_BATCH_SIZE = 6;

/** Footer browse panel: links per column before expanding. */
export const FOOTER_COLUMN_INITIAL_VISIBLE = 8;

export const FOOTER_COLUMN_BATCH_SIZE = 8;

/** All PDF Tools dashboard: links per category before expanding. */
export const TOOLS_DIRECTORY_INITIAL_VISIBLE = 8;

export const TOOLS_DIRECTORY_BATCH_SIZE = 8;
